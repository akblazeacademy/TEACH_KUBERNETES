apiVersion: v1
kind: Pod
metadata:
  name: pod-only-cpu
  namespace: rq-demo
spec:
  containers:
  - name: busybox
    image: busybox
    command: ["sleep", "3600"]
    resources:
      requests:
        cpu: "100m"
      limits:
        cpu: "200m"
