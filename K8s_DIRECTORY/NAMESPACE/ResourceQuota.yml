apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute-quota
  namespace: rq-demo
spec:
  hard:
    pods: "6"
    requests.cpu: "1"
    requests.memory: "2Gi"
    limits.cpu: "2"
    limits.memory: "4Gi"
