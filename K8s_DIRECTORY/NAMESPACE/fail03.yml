apiVersion: v1
kind: Pod
metadata:
  name: pod-heavy-request
  namespace: rq-demo
spec:
  containers:
  - name: busybox
    image: busybox
    command: ["sleep", "3600"]
    resources:
      requests:
        cpu: "2"
        memory: "3Gi"
