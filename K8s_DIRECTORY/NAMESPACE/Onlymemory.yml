apiVersion: v1
kind: Pod
metadata:
  name: pod-only-memory
  namespace: rq-demo
spec:
  containers:
  - name: busybox
    image: busybox
    command: ["sleep", "3600"]
    resources:
      requests:
        memory: "256Mi"
      limits:
        memory: "512Mi"
